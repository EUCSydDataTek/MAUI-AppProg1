<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppProg1.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ContentPage.ToolbarItems>
        <ToolbarItem Clicked="menuDetails_Clicked" Text="Details" />
        <ToolbarItem Clicked="menuDetailsModal_Clicked" Text="DetailsModal" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            VerticalOptions="Center">

            <Image
                HeightRequest="200"
                HorizontalOptions="Center"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                Source="dotnet_bot.png" />

            <Entry x:Name="entryName" Placeholder="Name" />

            <Button
                x:Name="btnGotoDetails"
                Clicked="btnGotoDetails_Clicked"
                HorizontalOptions="Center"
                Text="Goto DetailsPage with parameter" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
